---
description: Minecraft 指令/控制台命令以独立 plaintext 行输出，便于复制
alwaysApply: true
---

# Minecraft 指令输出格式

凡回复中出现 **Minecraft 相关可执行指令**（游戏内 `/` 命令、服务器控制台命令、Skript/插件管理命令等），必须像论文里的**独立公式**一样单独展示，方便用户一键复制。

## 必须遵守

1. **禁止**把指令嵌在正文句子里用行内 `` `反引号` `` 带过（说明文字与指令分行）。
2. 每条可复制的指令**独占一行**，前后各空一行；多条指令时**一条一块**，不要挤在同一段落。
3. 使用 **plaintext 代码块**（语言标记写 `text`，或无语言标记），块内只放指令本身，不加注释、不加 `$`、不加 `>` 提示符。
4. 控制台命令**不要**加前导 `/`；游戏内命令**保留**前导 `/`。
5. 若需说明「在哪里输入」，写在指令块**上方**的正文里，不要写进块内。

## 示例

### ✅ 正确

要在游戏内或控制台重启网页服务，先确认有 OP 权限，然后执行：

```text
/mcwws-web-reload
```

停止整个 Minecraft 服务器，在控制台输入：

```text
stop
```

停止后再启动服务器：

```text
start.bat
```

### ❌ 错误

请在游戏里输入 `/mcwws-web-reload` 重启网页。

请先 `stop` 再运行 `start.bat`。

## 适用范围

- 游戏内玩家/管理员命令（如 `/res tp`、`/shop reload`）
- Paper/Spigot 控制台命令（如 `stop`、`whitelist add`）
- 本仓库插件命令（如 `/mcwws-web-reload`、`/mcwws-web-status`）
- 与 Minecraft 运维直接相关的单行可执行文本

## 不适用

- 代码里的字符串、配置文件键值、Java/Skript 源码 — 仍按正常代码引用格式处理
- 纯概念说明、不含可复制命令的长解释段落
