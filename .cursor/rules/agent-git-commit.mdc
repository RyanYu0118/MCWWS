---
description: Agent 提交 Git 时仅包含本会话有意修改的文件，忽略服务器运行噪音
alwaysApply: true
---

# Agent 主导的 Git 提交规范

本仓库 Minecraft 服务器**后台持续运行**，会产生大量与开发无关的变更（物价、玩家数据、区块、世界状态等）。**禁止**仅凭 `git status` / 全量 diff / 文件修改时间生成提交说明。

## 何时提交

- **仅当用户明确同意**才执行 git commit / push；不要静默自动 push。
- 完成一轮**代码/配置开发**后，必须用 **AskQuestion** 弹出可点击选项（不要只让用户打字）：
  - **提交并推送 (y)**（Recommended）→ `git commit` + `git push`
  - **只提交 (c)** → 仅 `git commit`
  - **暂不提交 (n)** → 跳过
- 用户若已回复，以下**单行简写视为等同点击**（不区分大小写）：
  - `y` / `yes` / `提交并推送` → 提交并推送
  - `c` / `commit` / `只提交` → 只提交
  - `n` / `no` / `跳过` → 不提交

**说明：** Cursor 聊天无法提供常驻自定义按钮；AskQuestion 选项卡即「可点击按钮」。用户随时发 `y` 即可触发提交并推送，无需打完整句子。

## 暂存范围（最重要）

提交时**只 `git add` 本会话中 Agent 或用户明确修改过的路径**，例如：

- `tools/**` 源码与 build 脚本
- `plugins/Skript/scripts/**`
- 自研插件 jar 的源码与 `plugin.yml`（`tools/mcwws-*`、`MCWWS_*`）
- 用户点名要纳入的配置（如 `plugins/FastAsyncWorldEdit/config.yml`）

**默认不暂存**（除非用户明确要求或本会话专门改它们）：

- `level.dat`、`level.dat_old`、`raids.dat`、`raids_end.dat`
- `**/*.mca`
- `plugins/**/market_state.yml`、`price_history.csv`、`web_prices.yml`
- 玩家数据、日志、缓存、`.paper-remapped/**`
- 其他明显由服务器运行时写入的文件

若不确定某文件是否应纳入，**先列出拟提交文件清单问用户**，不要整仓 `git add .`。

## 提交信息怎么写

1. **以本会话实际完成的开发内容为准**（Agent 记忆中的改动意图），不是全仓库 diff 的统计结果。
2. 遵循根目录 `.cursorrules` 的**双语 Conventional Commits** 格式（中文标题 + 英文标题；复杂变更附中英列表正文）。
3. 正文描述**逻辑变更**（做了什么、为什么），不要罗列被忽略的运行时文件。
4. 若必须附带动态状态文件，放在正文**最后一条**并一笔带过。

## 推荐流程

1. `git status` / `git diff -- <本会话文件列表>` 核对
2. 仅 `git add` 确认过的路径
3. 用 HEREDOC 写入双语 commit message
4. `git commit` 后 `git status` 验证
5. 用户选「提交并推送」或回复 `y` 时再 `git push`
