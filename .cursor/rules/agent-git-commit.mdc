---
description: Agent 提交 Git 时以开发改动为主，运行时噪声一并推送；y 时若涉及服内玩法则同步写入 BookNews
alwaysApply: true
---

# Agent 主导的 Git 提交规范

本仓库 Minecraft 服务器**后台持续运行**，会产生大量与开发并行的变更（物价、玩家数据、区块、世界状态等）。

## 何时提交

- **仅当用户明确同意**才执行 git commit / push；不要静默自动 push。
- 完成一轮**代码/配置开发**后，必须用 **AskQuestion** 弹出可点击选项（不要只让用户打字）：
  - **提交并推送 (y)**（Recommended）→ `git commit` + `git push`，并按下方「BookNews 同步」处理
  - **只提交 (c)** → 仅 `git commit`（**不**改 BookNews，除非用户另说）
  - **暂不提交 (n)** → 跳过
- 用户若已回复，以下**单行简写视为等同点击**（不区分大小写）：
  - `y` / `yes` / `提交并推送` → **等同「提交并推送 (y)」**（含 BookNews 同步）
  - `c` / `commit` / `只提交` → 只提交
  - `n` / `no` / `跳过` → 不提交

**说明：** Cursor 聊天无法提供常驻自定义按钮；AskQuestion 选项卡即「可点击按钮」。用户随时发 `y` 即可触发提交并推送，无需打完整句子。

## BookNews 同步（仅 `y` / 提交并推送）

用户约定：**`y` = 提交 + 推送**；若本次推送内容**与服务器玩法/配置相关**（玩家或管理员能感知），还必须把变更写进 BookNews。配置为 **`Open-Book-Onjoin.always: true`**，玩家**每次进服**都会打开告示书。

### 何时写入

**要写**（任一命中即可）：

- 自研插件、Skript、DeluxeMenus、商店/经济/领地/传送/指南针等服内行为变更
- 玩家可见的命令、权限默认值、扣费、限额、交互步骤
- 已同步进 `WIKI.md` 的玩家向说明
- 新功能上线、bug 修复、重要运维公告（白名单、活动等）

**不要写**：

- 纯运行时噪声（物价快照、玩家数据、日志、区块、DB）
- 仅 Cursor 规则 / 构建脚本 / 与服内体验无关的仓库杂项
- 用户明确说「不要发告示」时

### 怎么写

1. **改** `plugins/BookNews/config.yml` 的 `book:` 正文（中文、书本页格式；保留翻页占位符如 `%booknews_gotopage2%`）。
2. **首页第一行必须写发布时间**，精确到秒，格式固定为：

```text
发布：yyyy年MM月dd日 HH:mm:ss
```

示例：`发布：2026年08月24日 23:12:54`。时间为**写入告示当下**的本地时间（北京时间），不要用模糊日期或省略秒。
3. **首页紧接发布时间后写清本期要点**（新功能 / 修复 / 注意），篇幅控制在玩家能翻完；细节可指向指南针或 Wiki。
4. 保持 `Open-Book-Onjoin.enable: true` 与 **`always: true`**（每次进服弹书）；不要改回 `always: false`，除非用户明确要求。
5. 将 `config.yml`（及必要的 BookNews 相关文件）**纳入同一次** `git add` / commit。
6. 推送成功后，若服在运行且 BookNews 已加载，执行（或请用户执行；管理页可点「重载服务器告示」）：

```text
/booknews reload
```

`always: true` 时一般**不必**再 `/booknews clear`（每次进服都会弹）；仅当需要强制刷新已读状态或排查问题时才清。

7. 若当时无法执行控制台命令（服已停、无权限等），在回复里用 plaintext 指令块写明 `/booknews reload`，提醒用户开服后执行。
8. 提交说明正文可带一句「同步 BookNews 玩家告示」，但告示正文以 `config.yml` 为准，不要把整本书粘进 commit message。

### 与 Wiki 的关系

- 玩家向规则仍优先改 `WIKI.md`（见 `wiki-sync.mdc`）。
- BookNews 是**短讯入口**：写「发生了什么 / 怎么用一句话」，详细步骤仍以 Wiki / 指南针为准。

## 暂存范围

提交时分两层处理：

### 1. 核心改动（优先描述）

本会话中 Agent 或用户**有意修改**的路径，例如：

- `tools/**` 源码与 build 脚本
- `plugins/Skript/scripts/**`
- 自研插件源码与 `plugin.yml`（`tools/mcwws-*`、`MCWWS_*`）
- `plugins/BookNews/config.yml`（当本轮按「BookNews 同步」写入时）
- 用户点名要纳入的配置

### 2. 运行时噪声（一并推送，提交说明次要带过）

**默认与核心改动一同暂存并推送**，不在正文中逐文件展开，只在列表**末尾用一条**概括，例如「同步服务器运行时状态与物价数据」。

常见噪声文件包括但不限于：

- `level.dat`、`level.dat_old`、`raids.dat`、`raids_end.dat`
- `**/*.mca`
- `plugins/**/market_state.yml`、`price_history.csv`、`web_prices.yml`
- 玩家数据、Essentials userdata、`usercache.json`
- 插件数据库/日志/缓存、`.paper-remapped/**`
- 其他明显由服务器运行时写入的已跟踪文件

推荐命令：

1. 先 `git add` 本会话核心路径（明确可控）
2. 再 **`git add -A`**（仓库根目录）纳入**全部**已跟踪变更与**未跟踪**的运行时/构建产物（Slimefun 备份与 error-reports、`tools/**/build/` 等）；**不要**仅用 `git add -u`（会漏掉 `??` 未跟踪文件）
3. 提交前 **`git status`** 应无剩余改动；若仍有 `??` 且非敏感文件，继续 `git add` 直至干净
4. **不要** `git add` 未跟踪的敏感文件（`.env`、密钥、凭证）；若出现应先警告用户

**禁止**仅凭 `git status` 统计或文件修改时间**代替**对核心改动的意图理解来撰写标题与正文主干。

## 提交信息怎么写

1. **标题与正文前几条以本会话实际完成的开发内容为准**（Agent 记忆中的改动意图）。
2. 遵循根目录 `.cursorrules` 的**双语 Conventional Commits** 格式（中文标题 + 英文标题；复杂变更附中英列表正文）。
3. 正文**先写**核心逻辑变更（做了什么、为什么），**不要**在正文前几项罗列噪声文件名。
4. 若有噪声文件一并提交，在中文列表与英文列表的**各自最后一条**用一句话带过，例如：
   - `- 同步服务器运行时状态、物价与市场数据等`
   - `- Synced server runtime state, prices, and market data`

## 推荐流程

1. `git status` / `git diff -- <核心路径>` 核对开发改动
2. 若走 **`y`** 且改动与服内玩法相关：先更新 `plugins/BookNews/config.yml` 告示正文
3. `git add` 核心路径（含 BookNews），再 `git add -A` 纳入噪声与未跟踪文件；`git status` 确认工作区干净
4. 用 HEREDOC / 临时 UTF-8 文件写入双语 commit message（噪声放末尾一条）
5. `git commit` 后 `git status` 验证
6. 用户选「提交并推送」或回复 `y` 时再 `git push`
7. 推送后对运行中的服执行 `/booknews reload`（或提示用户执行）；首页须含 `发布：yyyy年MM月dd日 HH:mm:ss`
