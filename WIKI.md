# 流浪世界（MCWWS）特色功能 Wiki 底稿

> 这份文档是给 Halo / 公开 Wiki 用的源稿，按**当前仓库实现**整理，不是聊天记忆的逐条复述。  
> 仓库：Paper **26.2** 服（由 1.21.11 存档升级，Git 历史连续）。对外短名 **MCWWS**，制作组 **RYAN STUDIO**。  
> 发布站点与嵌入商店的正式稿在 [RYAN-STUDIO](https://github.com/RyanYu0118/RYAN-STUDIO)。本文件随服务器代码一起改，避免 Wiki 和实现脱节。

---

## 0. 先说清楚：哪些能写进 Wiki，哪些不要写

**能写、而且应该写得很细的**

- 玩家在生存里能碰到的规则：扣费、回收、撤销、商店、网页下单、飞行、领地、地图规划
- 客户端模组要装什么、不装会怎样
- 权限默认值（尤其是「OP 也没有」的那些）
- 容易误触、容易误解的交互

**不要当特色宣传的**

- 运行时噪声：物价快照、玩家数据、区块、日志
- 某次 NPE / 某次搜索 GUI 空白的内部修复过程（可放「已知问题」一句）
- 管理员停服、换 jar、Git 提交规范

---

## 1. 服务器定位（一句话）

这是一台**始终生存**的 Paper 服：创造级建造工具（创世神、Axiom Editor）可以用，但每一格都按**动态市场**付钱或回收。经济、网页商城、游戏内商店、零钱明细、领地和三维地图是同一套物价在转。

普通生存交互（村民、展示框、盔甲架、门）优先于建造辅助 UI。建造工具不能默认抢走右键。

---



## 2. 给玩家看的特色总览


| 特色       | 玩家实际感受到什么                                  |
| -------- | ------------------------------------------ |
| 动态市场     | 买贵卖便宜会随库存和近期买卖波动；游戏内商店和网页商店同一套价            |
| 生存创世神    | `//set` 等指令按格扣材料费 + 劳务费；拆除按市场卖价折现          |
| 生存 Axiom | 装客户端模组后，生存里也能开 Editor；扣费规则与创世神对齐           |
| 搬运只收劳务   | 同一次编辑里「这里拆、那里放」的同种方块，不走市场                  |
| 撤销退 95%  | `//undo` / Axiom 撤销：退上一笔，扣 5% 手续费，市场库存一并还原 |
| 零钱明细     | 网页账本记录建造、商店、飞行等；毫秒时间戳                      |
| 网页商城     | 购物车可手输数量，单次最多 10000；下单后进游戏背包系统领取           |
| 游戏内商店    | UltimateShop：分类跟 26.2 创造栏走；搜索、收藏；文字搜索要点漏斗再在聊天栏输入 |
| 光源可拆可调   | 生存能敲掉 Light 方块并掉落；右键能调亮度                   |
| 三维地图 GIS | BlueMap 上规划道路/建筑、导入 Litematica、自由漫游        |
| 领地中文     | Residence 权限旗标与拒绝提示是中文                     |


---



## 3. 动态市场经济



### 3.1 玩家需要知道的规则

- **买入价**和**卖出价**始终用同一个市场倍率，避免只涨买价、不涨卖价来套利。
- 安全钳制：回收价不会高于买入价；若算出来卖价更高，会压到买入价的 **99%**。
- 价格保留两位小数。
- 分类微调（叠在动态倍率上）：
  - 考古类（`archaeology`）：整体 **+10%**
  - 木材类（`wood`）：整体 **-10%**
- 游戏内商店、网页商店、创世神/Axiom 材料费，读的是同一份导出物价 `web_prices.yml`。



### 3.2 动态倍率怎么算（写进「经济说明」页即可）

基准价来自 `plugins/Skript/scripts/mcwws/economy/database/items.yml` 的 `unit_buy` / `unit_sell`。

动态因子（可配置，当前默认）：

```
dynamic_factor = clamp(
  1
  + 0.35 * (均衡库存 - 当前库存) / 均衡库存
  + 0.18 * 买入压力 / 256
  - 0.18 * 卖出压力 / 256,
  0.5, 2.5
)
```

含义：

- 库存低于均衡 → 变贵；高于均衡 → 变便宜
- 最近有人抢着买 → 变贵；有人大量抛售 → 变便宜
- 买卖压力每分钟衰减到约 **82%**
- 库存每分钟向均衡回归 **0.5%**（模拟自然补货/消耗）
- 默认均衡库存 ≈ `10000 / 基准买入单价`（单价缺失时用 512）

玩家买入会减少虚拟库存、增加买入压力；卖出相反。创世神/Axiom **放置**按买入侧扣库存，**拆除回收**按卖出侧加库存。

### 3.3 和建造的关系

建造不是「另开一套价」，而是：

- 放置：按市场**买价**扣材料，并减少该物品虚拟库存
- 拆除：按市场**卖价 × 回收率（当前 100%）**打给玩家，并增加虚拟库存
- 搬运：同种方块拆了又放，**不改库存**

超大编辑会被 Axiom 拆成多个网络包，跨包的搬运无法对冲，只会在**同一个包**里对冲。

---



## 4. 生存创世神（WorldEdit / FAWE）

插件：`MCWWS_WorldEditSurvival`  
权限：`mcwws.we.survival.bypass` 默认 **false**（含 OP，不会因为是管理员就免费）

### 4.1 何时扣费

纳入生存扣费的指令包括：`set`、`stack`、`replace`、`replacenear`、`fill`、`walls`、`overlay`、`repl`。未列入的指令会直接取消并提示未纳入扣费。

仅生存 / 冒险可用。请先选区。选区超过 **50 万格**会拒绝扫描。

### 4.2 费用公式

一次指令净额：

```
净额 = 材料费 + 劳务费 − 拆除回收
```

- **劳务费**
  - 放置：每格 **0.5**
  - 拆除：每格 **1.0**（放置单价 × 2）
- **材料费**：新放上去的方块 × 当前市场买价
- **拆除回收**：拆下来的方块 × 当前市场卖价 × 回收率 1.0
- **搬运**：同种方块在同一次指令里拆+放，只收劳务，不走市场
- 净额为负时，差额打给玩家（拆贵重方块换廉价方块会赚钱）

选区内的 **Slimefun 机器**和不可破坏方块会保留，不计费。

### 4.3 撤销 `//undo`

- 识别为「撤销上一笔建造」时：退还扣款，收 **5%** 手续费，市场库存同步还原
- 若上一笔是拆除净收入：会**收回**回收款（同样扣手续费口径），余额不够收回时会失败并提示找管理员
- 没有对应扣费记录时，仍可能只还原市场库存

聊天前缀：`[创世神]`。

### 4.4 玩家侧注意

- 改选区之后立刻 `//set`：系统必须重新扫描，不能沿用旧的「0 格变更」估计，否则会出现免费建造。这是已经修过的坑，Wiki 里可以写成「改选区后请以聊天里的扣费提示为准」。
- 连续两次 `//set` 会各扣一次，不是只扣第一笔。

管理员重载：

```text
/mcwws-we-reload
```

---



## 5. 生存 Axiom Editor

这是本服和「原版 Axiom 创造服」差别最大的一块。

### 5.1 要装什么


| 端   | 名称                          | 当前版本           |
| --- | --------------------------- | -------------- |
| 服务端 | AxiomPaper                  | 5.0.4（MC 26.2） |
| 服务端 | `MCWWS_AxiomSurvival`       | 1.1.2          |
| 客户端 | Axiom                       | 5.5.0（MC 26.2） |
| 客户端 | `MCWWS_AxiomSurvivalClient` | 1.2.4          |


没有客户端模组时：服务端仍会拦创造切换、按格扣费，但 Editor 菜单/生存 HUD/飞行权威值对不齐，体验会明显差一截。连上模组后聊天会提示「已启用生存 Editor」。

握手条件：

- 有 `mcwws.axiom.survival.use`（默认给所有人）
- 当前是生存或冒险
- 余额不低于配置的下限（当前 **0**，即不拦）

`axiom.player.gamemode.creative` 和 `axiom.player.gamemode.spectator` 会从 default 组撤掉，避免 Axiom 把玩家真切到创造/旁观。default 组会开放 `axiom.*` 其余建造能力，**扣费仍走 MCWWS**。

免费通行证 `mcwws.axiom.survival.bypass` 默认 **false**，OP 也没有。

### 5.2 会话怎么走（玩家流程）

1. 服务端玩家**始终是生存**（菜单打开的那几秒服务端会切旁观，关菜单立刻切回）。
2. 打开 Editor 菜单：客户端本地旁观；服务端快照位置、朝向、飞行状态。
3. 关掉菜单进入建造：本地假装创造以便 Axiom 工具槽出现，但：
  - 飞行以服务端为准（FlyWithFood / 玩家自己开的飞）
  - `E` 打开的是**生存背包**，不是创造物品栏
  - 挖掘按生存进度，不是秒破
  - 饱食度满时不会播进食动作
4. 完全退出 Editor：位置、模式、飞行按进入前恢复。

进入 Editor **不会**自动帮你开飞，退出也**不会**强行关飞，以免覆盖玩家自己的飞行选择。

卡住飞不回来或位置不对时：

```text
/axiomrestore
```

诊断权限与余额：

```text
/axiomcheck
```



### 5.3 扣费（与创世神对齐）

同样是：材料 + 劳务 − 回收；搬运只收劳务；撤销退 95%。

额外：

- **实体**：生成每只劳务 **20**，删除 **5**，调整 **2**（不扣材料）
- **生物群系画笔**：每格劳务 **0.02**
- 生存里**不能**用 Axiom 改世界时间、世界属性，也不能切创造
- 受保护方块（Slimefun / 不可破坏）：写入后按快照还原，这些格不计费；一次超过 **4096** 个受保护方块则整包取消
- 扫描上限同样 **50 万格**
- 提示里会带「最近处理方块离你多少格」，方便发现误触了远处

Axiom 会把超大笔刷拆成多个包，所以：

- 额度「单次上限」实际是**单个包**的上限（当前未启用，全是 0 = 不限）
- 跨包搬运无法对冲
- 撤销按「放置/拆除计数对调且格数相同 + 10 分钟内」配对；重做按新建造扣费

聊天前缀：`[Axiom]`。扣费提示会在约 1 秒内合并成一条。

### 5.4 实体操纵小方块（容易误触）

生存里为了让工具槽出现，客户端会把 Axiom 判成「已激活」。副作用是：附近的展示实体 / 标记会画出**可拖动的小方块**，右键容易被抢走（村民、展示框、盔甲架都中招）。

**默认关闭。** 权限 `mcwws.axiom.survival.entity` 默认 false，**OP 也没有**。

需要给建造者开：

```text
/lp user <玩家名> permission set mcwws.axiom.survival.entity true
```

关掉：

```text
/lp user <玩家名> permission unset mcwws.axiom.survival.entity
```

改权限后约 2 秒内自动同步，不用重进服。必须使用更新后的客户端模组 1.2.4+。

### 5.5 客户端其他注意

- Axiom 配置里的 Fast Place：在生存里若开启，可能让「擦到门板边缘」的右键变成放置/超出距离而点不开门。普通生存建议关掉 Fast Place。
- 无限触及、Phantom、强制放置等能力在服务端配置里默认关，且受 Axiom 权限约束。

---



## 6. 零钱明细（钱包账本）

两层一起工作：

1. **Skript** `player_ledger.sk`：飞行消耗、商店等写入 `ledger_queue.txt`
2. **插件** `MCWWS_EconomyLedger`：兜住 Essentials 余额变动，避免漏记；同一笔 4 秒内不重复写

网页账本：

- 独立标签打开
- 当前余额约每秒刷新
- 时间戳到毫秒
- 飞行这种每秒扣一次的，网页侧会把约 2 秒内的合并成一行

屏幕左下角浮层（装了 Axiom 生存客户端时）目前配置为**关闭**；未装模组的玩家可回退到 action bar。聊天里各插件自己的 `[创世神]` / `[Axiom]` 提示仍然在。

分类名（账本里能见到的）：飞行消耗、Axiom 建造 / 撤销 / 拆除回收、创世神建造 / 撤销 / 拆除回收、购买 / 出售、转账、其他。

---



## 7. 游戏内商店（UltimateShop Premium）

- 主菜单有**搜索**（漏斗）和**收藏**
- 打开搜索：

```text
/shop searchgui
```

或 `/shop menu search`

**文字搜索用法（务必写进 Wiki）：**

1. 点界面上方的漏斗
2. 菜单会关掉
3. 在**聊天栏**输入物品名（中文译名可以，例如「橡木」）
4. 搜索界面重新打开并列出结果

把物品放进筛选槽是按物品匹配，不需要打字。

Paper 26.2 上商店物品取中文名时曾会崩溃，表现为「匹配数是 1、格子却是空的」。服务端插件 `MCWWS_UltimateShopFix` 已补上，不需要玩家操作。

商店分类与 **Java 26.2 原版创造物品栏**对齐（建筑 / 彩色 / 自然 / 功能 / 红石 / 工具与实用 / 战斗 / 食物 / 原材料）。页内顺序跟创造栏一样，例如橡木是：原木 → 木头 → 去皮原木 → 去皮木头 → 木板 → 楼梯 → 台阶 → 栅栏……再轮到云杉。

和创造栏一样，**同一物品可以出现在多个分类**：例如橡木门既在建筑方块也在红石方块。两边买价、卖价相同。商店搜索会列出每一处分页里的同一件货。网页下单仍走其中一处报价，价格不变。

额外保留的店：

- **酿造**：原版创造里药水是带 NBT 的变体，本服按效果和等级单独标价
- **附魔**：附魔书同样按等级拆开卖
- **每日商店**、**MCWWS**（光源等服内特有物品）

刷怪蛋和管理员方块（屏障、命令方块）不进商店。

商店物价占位符走 `%mcwws.price_buy_…%` / `%mcwws.price_sell_…%`，与网页同步。管理员改完商店文件后，在游戏内重载：

```text
/shop reload
```

网页侧的分类列表随网页热重载更新：

```text
/mcwws-web-reload
```

---



## 8. 网页商城与网页服务

服务端插件 `MCWWS_WebHost`：Minecraft 进程在时拉起 Node 网页，关掉 Minecraft 时一并停。不重启游戏的热重载：

```text
/mcwws-web-reload
```

```text
/mcwws-web-status
```

玩家侧：

- 购物车数量可直接输入，单件上限 **10000**，提交时会再校验一次
- 下单后生成「待领取订单」，玩家上线后由脚本写入 **BetterBags**（不是撒一地物品）
- 交易会记进网页仪表板日志，并进入零钱明细

网页商店也可以嵌在 Halo Wiki 里（全宽、隐藏目录），那是发布站配置，不是游戏内命令。

**投影粘贴：** 网页付款 → 选定锚点 → 玩家在线时以**玩家本人身份**执行 WorldEdit 粘贴，因此同样走生存创世神扣费与保护规则。相关命令（管理/网页回调，不必写在玩家首页）：`/build`、`/mcwwswebpaste`、`/mcwwspasteasplayer`。

---



## 9. 背包（BetterBags）

本服改过默认行为，Wiki 必须写，否则玩家会以为「捡东西自动有头颅背包」：

- 禁止除 `/bags give` 外自动发放背包头颅
- 可售物品的自动入包由 MCWWS 脚本接管
- 没有授权头颅时，不会因为捡起可售物就送一个新背包

管理员回收/重发相关：`/mcwws-bags-reclaim`（管理用）。

---



## 10. 光源（Light 方块）

生存/冒险：

- **左键**敲光源：按玩家破坏事件处理（领地/保护会拦），会掉落
- **站立右键**已放置的光源：调节亮度 0–15
- **手持光源对空气右键**：选放置亮度（默认 15）；物品外观仍是原版

世界里的图标按真实亮度换贴图，不是全部一个样子。

---



## 11. 领地（Residence）

- 权限旗标名称与拒绝消息为**中文**
- 服务器地主账号为 **MCWWS**（用于公共领地归属，不是让玩家把家过户给管理员私人号）

具体旗标表以游戏内 `/res` 说明和语言文件为准；Wiki 可另开「领地旗标对照」页从 `plugins/Residence/Language/Chinese.yml` 抄表。

---



## 12. 三维地图 / GIS（BlueMap）

文件：`bluemap/web/js/mcwws-gis.js`（当前构建号 `20260610-117`），API 走主机 **8002** 端口。

玩家/规划向功能（公开 Wiki 可写「地图能做什么」）：

- 自由飞行漫游
- 道路 / 区域绘制、路名、建筑名
- 折线插入、路段方向箭头
- 多点选择、重心、三维 gizmo 轴拖
- 套索选择
- 导入 Litematica 材料列表做规划
- 日夜/图层面板
- 大坐标用 10000 格分块，避免 WebGL float 精度把远处建筑抖没

地图上的商店图层见 `mcwws-shops.js`（和游戏内/网页商店呼应）。

---



## 13. 其它生存便利

- **CustomCrafting 配方书：** default 组已给配方书分类总览权限，普通玩家能打开分类，而不是只有 OP 能看
- **创世神选区/堆叠：** default 组有 `worldedit.analysis.sel` 与 `worldedit.region.stack`
- **DeluxeMenus 管理界面：** 重载插件后会尝试把「服务器管理」菜单重新打开，避免管理员卡在空白界面

---



## 14. 权限速查（写进管理员附录）


| 节点                            | 默认               | 含义                                           |
| ----------------------------- | ---------------- | -------------------------------------------- |
| `mcwws.axiom.survival.use`    | true             | 允许生存 Axiom 付费建造                              |
| `mcwws.axiom.survival.entity` | **false**（OP 也无） | 显示实体拖动小方块                                    |
| `mcwws.axiom.survival.bypass` | **false**（OP 也无） | 跳过 Axiom 扣费与保护扫描                             |
| `mcwws.axiom.survival.admin`  | op               | 重载 Axiom 生存配置                                |
| `mcwws.we.survival.bypass`    | **false**（OP 也无） | 跳过创世神扣费与保护扫描                                 |
| `mcwws.we.survival.admin`     | op               | 重载创世神生存配置                                    |
| `mcwws.ledger.admin`          | op               | 重载零钱明细插件                                     |
| `mcwws.web.admin`             | op               | 网页服务重载/状态                                    |
| `axiom.*`                     | default 组脚本授予    | 建造能力；创造/旁观节点会被 unset                         |
| `axiom.entity.manipulate`     | 随 `axiom.*`      | 有能力不等于显示小方块，还要 `mcwws.axiom.survival.entity` |


经济管理（Skript，建议不要写进玩家页）：

```text
/eco-reload
```

```text
/eco-check
```

```text
/eco-market-reload
```

```text
/eco-market-info
```

```text
/eco-market-reset
```

```text
/eco-market-reset-all
```

---



## 15. 建议的公开 Wiki 目录

1. **欢迎 / 服务器是什么** — 用第 1、2 节
2. **钱怎么算** — 第 3 节（公式放折叠或「进阶」）
3. **生存建造：创世神** — 第 4 节
4. **生存建造：Axiom** — 第 5 节（含模组安装、小方块权限、Fast Place）
5. **零钱明细** — 第 6 节
6. **游戏内商店** — 第 7 节（搜索一定要写「点漏斗后打聊天」）
7. **网页商城** — 第 8 节
8. **背包与光源** — 第 9、10 节
9. **领地** — 第 11 节 + 旗标表
10. **地图规划工具** — 第 12 节
11. **管理员附录** — 第 14 节、模组版本表、重载命令

Halo 嵌入商店页：全宽、藏 TOC，只留商城本体。

---



## 16. 和代码的对应关系（给以后改 Wiki 的人）


| 主题               | 主要位置                                                                                                 |
| ---------------- | ---------------------------------------------------------------------------------------------------- |
| 动态价格             | `plugins/Skript/scripts/mcwws/economy/economy.sk`、`z_economy_dynamic.sk`、`market_dynamic_config.yml` |
| 导出给网页/建造的价       | `plugins/Skript/scripts/web/mcwws/economy/web_prices.yml`                                            |
| 创世神扣费            | `tools/mcwws-worldedit-survival/`                                                                    |
| Axiom 扣费与 Editor | `tools/mcwws-axiom-survival/`                                                                        |
| Axiom 客户端        | `tools/mcwws-axiom-survival-client/`                                                                 |
| 商店搜索崩溃补丁         | `tools/mcwws-ultimateshop-fix/`                                                                      |
| 商店创造栏分类           | `plugins/Skript/scripts/web/scripts/sync-ultimateshop-from-items.js`、`creative_tabs_26.2.json`       |
| 零钱明细兜底           | `tools/mcwws-economy-ledger/`                                                                        |
| 网页进程             | `tools/mcwws-web-host/`                                                                              |
| 网页下单入包           | `plugins/Skript/scripts/mcwws/shop/web_pending_delivery.sk`                                          |
| GIS              | `bluemap/web/js/mcwws-gis.js`                                                                        |


改了扣费数字、权限默认值或搜索交互，先改这里再同步 Halo。